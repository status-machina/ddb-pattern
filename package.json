{
  "name": "@status-machina/ddb-pattern",
  "version": "0.0.2",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"No tests specified\" && exit 0",
    "prepare": "npm run build"
  },
  "peerDependencies": {
    "@aws-sdk/lib-dynamodb": "^3.x.x",
    "ulidx": "^2.x.x"
  },
  "devDependencies": {
    "typescript": "^5.x.x"
  },
  "publishConfig": {
    "access": "public"
  }
}
